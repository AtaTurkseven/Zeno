pyyaml>=6.0
pyserial>=3.5
numpy>=1.24
requests>=2.31

# Optional — Vision support (camera capture)
# opencv-python-headless>=4.8

# Optional — Voice support (speech recognition + text-to-speech)
# SpeechRecognition>=3.10
# pyaudio>=0.2.13
# pyttsx3>=2.90

# Optional — Real-time voice call system (zeno/audio/)
# sounddevice>=0.4.6   # non-blocking microphone capture + playback
# kokoro>=0.9.2        # Kokoro neural TTS engine
